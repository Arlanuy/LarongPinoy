<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @quiz.name %>
</p>

<p>
  <strong>User_ID:</strong>
  <%= @quiz.user.email.split('@')[0].capitalize %>
</p>

<p>
  <strong>User_Score:</strong>
  <%= @quiz.user_quiz.score %>
</p>
<%= link_to 'Start Quiz', Question.where(quiz_id: @quiz.id).first %>
<%= link_to 'Edit', edit_quiz_path(@quiz) %> |
<%= link_to 'Back', quizzes_path %>
